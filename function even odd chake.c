#include<stdio.h>
int main()
{
   int arr[20],n,i;
scanf("%d", &n);
   for(i=0;i<n;i++)
      scanf("%d", &arr[i]);
   f(arr[i]);
}
void f(int ar[],int n)
{
   int i;
   for(i=0;i<n;i++)
printf("%d", ar[i]);
}
